// 14. Longest Common Prefix

#include <iostream>

int main()
{
    std::string strs[] = {"flower", "flow", "flight"};
    // strs = ["c", "acc", "ccc"]
    // strs = ["dog","racecar","car"]

    // for (int i = 0; i < sizeof(strs); i++)
    // {
    //     std::cout << strs[i] << i << std::endl;
    // }
    std::cout << sizeof(strs) / sizeof(strs[0]) <<sizeof(strs) << sizeof(strs[0]) << std::endl;
}

// an array of strings
